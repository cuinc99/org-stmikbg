<% provide(:page_title, "| All Events") %>

<% provide(:events_active, "active") %>

<% provide(:page_header, "All Events") %>
<% provide(:optional_description, "Listing for all events") %>

<% provide(:level, "All Events") %>

<div class="box">
  <div class="box-header with-border">
    <%= link_to new_authors_event_path, method: :get, class: "btn btn-default btn-sm" do %>
      <i class="fa fa-pencil-square-o"> New Event</i>
    <% end %>

    <div class="box-tools">

        <%= form_tag authors_events_path, method: :get, class: "input-group input-group-sm", style: "width: 150px;" do %>
          <%= text_field_tag :search, params[:search], class: "form-control pull-right", placeholder: "Search"  %>
          <div class="input-group-btn">
            <%= submit_tag "Search", class: "btn btn-default" %>
          </div>
        <% end %>

    </div>

  </div>
  <!-- /.box-header -->
  <div class="box-body">
    <table class="table table-bordered">
      <tr>
        <th style="width: 10px">#</th>
        <th>Title</th>
        <th>Status</th>
        <th>Published at</th>
        <th>Option</th>
      </tr>

      <%= render 'post' %>

    </table>
  </div>
  <!-- /.box-body -->
  <div class="box-footer clearfix">
    <div class="apple_pagination">
    <div class="page_info">
      <%= page_entries_info @events %>
    </div>
    <%= will_paginate @events, :container => false %>
  </div>
  </div>
</div>
